---
'react-json-tree': minor
---

Render expand/collapse arrows as focusable `<button>` elements with `aria-expanded`, `aria-controls`, and `aria-label` for keyboard and screen-reader access, and add an `ArrowComponentOverride` prop to supply a custom arrow component.
