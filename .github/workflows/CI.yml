name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v2
      - working-directory: ./extension
        run: yarn install --immutable
      - working-directory: ./extension
        run: yarn test
